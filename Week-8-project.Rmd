# Week 8 Project

## Wendy Chu

<!-- leave this material as is -->
Compiled on `r date()`.

```{r include = FALSE}
library(DataComputing)
library(ggdendro)
```
<!-- put your content after this line -->


```{r}
Dists <- dist(mtcars)
Dendrogram <- hclust(Dists)
ddata <- dendro_data(Dendrogram)
ggdendrogram(Dendrogram, rotate = TRUE) + geom_text(data=ddata$labels, aes (x=x, y=y, label=label), vjust=0)

```


```{r}
NCI60new <-
  NCI60 %>%
    gather(key=cellLine, value=expression, -Probe)
```

```{r}
Newtable <-
  NCI60new %>%
  group_by(Probe, cellLine) %>%
  summarise(expression=mean(expression)) %>%
  ungroup ()
```

```{r}
Newtable2 <-
  NCI60new %>%
  group_by(Probe) %>%
  summarise(spread=sd(expression))
```

```{r}
Newtable3 <-
  Newtable %>%
  group_by(Probe) %>%
  filter(sd(expression)>=4)
```


```{r}
Newtable4 <-
  Newtable3 %>%
  spread(key=Probe, value=expression)
```


```{r}
row.names(Newtable4) <- Newtable4$cellLine
```

```{r}
Distances <-
  dist(Newtable4 %>% select(-cellLine))
```

```{r}
Dendrogram2 <- hclust(Distances)
ddata <- dendro_data(Dendrogram2)
ggdendrogram(Dendrogram2, rotate = TRUE)
```

